
import java.util.ArrayList;
import java.util.List;

public class test {

	/*
	ArrayList list = new ArrayList();
	list.add("one");
	list.add("one");
	list.add("one");
	list.add("one");
	int l=0;
	list.add(l);
	for (int a=0; a<list.size(); a++) {
		System.out.print(list.get(a));
	}
/*	String one = "test this out";
String[] splits= one.split(" ");
String[] rev= new String [100] ;
//System.out.print(splits.length);

for (int i=0; i<splits.length; i++) {
//System.out.print(splits[i]+ "\n");
rev[i]=splits[splits.length-1];
System.out.print(rev.toString());
}
System.out.print(rev.toString());
		List<String> test = new ArrayList<String>();
		test.add("one");
		test.add("two");
		test.add("three");
		test.add("owt");
		
		funWithAnagrams(test);
	}
	public static List<String> funWithAnagrams(List<String> s) {
	    // Write your code here
	        System.out.println(s.size());
	        
	        int i = 0;
	    for (i=0; i<s.size()-1;i++) {
	    	if((s.get(i)).length()==(s.get(i+1)).length()) {
	    		System.out.print(s.get(i));
	    	}
	    }
	    return s;

	    }
*

*
*/
	
}
